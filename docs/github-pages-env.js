// This script will be included in the build and helps configure the app for GitHub Pages
window.GITHUB_PAGES_ENV = {
  // Set this to the name of your GitHub repository. For example: '/my-repo/'
  // Leave as empty string if deployed at root domain
  basePath: '',
  isGitHubPages: true
};